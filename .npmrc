# Required for Expo config plugin resolution in pnpm monorepos.
# Expo's plugin resolver walks node_modules relative to the app directory;
# without hoisting it can't find plugins installed at the workspace root.
shamefully-hoist=true
